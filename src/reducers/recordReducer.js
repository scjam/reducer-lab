export const initialState = {
  before: [],
  current: [],
  after: []
};

// export default function reducer(state, action) {
//   switch(action.type) {
//     case typeName:
//       return { ...state, ...payload };

//     default:
//       return state;
//   }
// }
